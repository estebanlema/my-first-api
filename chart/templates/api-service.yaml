apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-first-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "my-first-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "my-first-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
